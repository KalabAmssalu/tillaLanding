"use client";

import React from "react";

import DiscoverPlans from "@/components/module/landing/DiscoverPlan";

type Props = {};

const MemeberScreen = (props: Props) => {
	return (
		<div>
			<DiscoverPlans />
		</div>
	);
};

export default MemeberScreen;

import React from "react";

import DiscoverPlans from "@/components/module/landing/DiscoverPlan";
import MemeberScreen from "@/components/screen/memberScreen/memeberScreen";

type Props = {};

const page = (props: Props) => {
	return (
		<div>
			<MemeberScreen />
		</div>
	);
};

export default page;
